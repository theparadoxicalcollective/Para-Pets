================================================================================
                         MASTER GAME DOCUMENT
                    GAME_SYSTEM_OVERVIEW — ParaPets
================================================================================
Last Updated: March 26, 2026
Purpose: This document exists so that any developer or AI assistant can read
it before touching the codebase and immediately understand the full scope of
the project — what it is, how it works, how each system connects, and what
pitfalls to watch for.

================================================================================
SECTION 1 — WHAT THE GAME IS
================================================================================

ParaPets is a browser-based fantasy RPG set in a world called Veridia.
Players hatch and raise magical creatures called ParaPets, battle enemies
across themed regions, go fishing with a real-time minigame, trade on a
player marketplace (the Mire Bazaar), and decorate a personal Pet House.

The tech stack is:
  Frontend  — React + TypeScript, Vite, Tailwind CSS, Wouter (routing),
               TanStack Query v5, Framer Motion, shadcn/ui, Lucide React
  Backend   — Node.js + Express + TypeScript, Drizzle ORM, Passport.js
  Database  — PostgreSQL (via Drizzle)
  Payments  — Stripe (client: server/stripeClient.ts, webhooks: server/webhookHandlers.ts)

The app is full-stack in a single repo. The Express server serves both the
REST API and the Vite-compiled frontend on the same port.

================================================================================
SECTION 2 — CORE GAMEPLAY LOOP
================================================================================

1. EARN COINS
   - Catch fish and sell them, or defeat enemies to collect coin drops.

2. BUY EGGS / GEAR
   - Spend coins at World Shops (region-specific) to buy pet eggs, fishing
     poles, bait, potions, accessories, and power-up consumables.

3. HATCH & RAISE
   - Eggs hatch over real time (hours). Use Hatch Accelerator items to speed
     this up. Once hatched, feed pets power-up items to raise their stats.

4. BATTLE
   - Take pets into region encounters. Slash enemies with swipe gestures, parry
     attacks, and fire special skills. Defeated enemies drop coins, XP, and
     rare items.

5. FISH
   - Equip a pole and optional bait. Complete the tension/reel minigame to catch
     fish for the Fish Book (collection) or sell them for coins.

6. SPECIALIZE
   - Better fishing gear → rarer fish → more coins.
   - Better battle gear / higher-level pets → tougher enemies → rarer drops.

7. ENDGAME
   - Complete the Fish Book, climb the PvP Arena rankings, unlock all Veridia
     regions, and fully outfit pets with accessories.

================================================================================
SECTION 3 — FISHING SYSTEM OVERVIEW
================================================================================

FILES:
  client/src/pages/FishingPage.tsx   — all minigame UI & logic
  server/routes.ts                   — cast, catch, server-side validation
  shared/schema.ts                   — shopItems (fish), playerFishInventory,
                                       playerFishingEquipment

--- 3.1 Equipment ---

POLES
  - Stored in user_inventory; linked to a shop_item with type "fishing" and
    fishingType "pole".
  - poleMaxUses: total durability. poleUsesLeft: remaining uses.
  - Every cast attempt (successful or not) decrements poleUsesLeft.
  - High-tier poles (3★-5★) have poleSlowdown3/4/5 multipliers that reduce
    how fast line tension rises, making harder fish manageable.
  - Some poles have rareCatchBoostPercent which increases the weight of 4★
    and 5★ fish in the selection pool.

BAIT
  - Consumable. One unit consumed only on a SUCCESSFUL catch.
  - baitCatchBoost: increases the progress gain rate during reeling.
  - baitRarityBoostStar + rarityBoostPercent: boosts the weight of a specific
    rarity tier in the pond's fish pool.

--- 3.2 Fishing Phases ---

  IDLE       → Player selects pole and bait.
  CASTING    → Animation; verifies pole is equipped and has durability.
  WAITING    → Random delay 1.5s–4s; server selects the fish from the pond.
  NIBBLE     → Player must tap/click within the window (shorter for rarer fish).
                 5★ fish: 1.1 s window, 1 chance.
                 1★ fish: 2.5 s window, 3 chances.
  REELING    → Core tension/reel minigame (see 3.3).
  CAUGHT/MISSED → Final outcome.

--- 3.3 Tension & Reel Minigame ---

Three simultaneous meters:

  CATCH PROGRESS (0–100%): Fills while player holds "Pull". Reach 100% → fish caught.
  TENSION       (0–100%): Rises while holding "Pull", falls when released.
                           Sweet spot is roughly 35%–65% for 5★ fish.
                           Hitting 100% → line snaps → fish lost.
  ESCAPE METER  (0–100%): Fills when player releases OR is outside sweet spot.
                           Hitting 100% → fish escapes.

Fish cycle through four behaviour states during the reel phase:
  CALM   — standard rates.
  RESIST — tension rises 50% faster, progress 30% slower.
  TIRED  — tension rises 50% slower, progress 50% faster (player's advantage).
  SURGE  — tension spikes at 2.2× rate, player MUST release to avoid snapping.

--- 3.4 Server-Side Catch Logic ---

  Base rarity weights: 1★:60  2★:24  3★:10  4★:4  5★:2
  Pole and bait modifiers adjust these weights before selection.
  Client sends a performanceScore to the server after the minigame.
  If performanceScore = 100 → guaranteed catch.
  If lower → catch chance = 0.4 + (score / 100) * 0.5

================================================================================
SECTION 4 — BATTLE / SLASH SYSTEM OVERVIEW
================================================================================

FILES:
  client/src/components/BattleArena.tsx  — PvE combat engine
  client/src/pages/PvpBattlePage.tsx     — PvP combat engine
  server/routes.ts                       — /api/explore/:locationId/encounter,
                                           /api/explore/defeat/:enemyId
  shared/schema.ts                       — locationEnemies, enemyDrops

--- 4.1 Slash / Hit Mechanics ---

  "Fruit Ninja"-style continuous swipe detection (not tap-to-hit).
  - onPointerDown starts recording a slashPath (up to 22 coordinate points).
  - Each frame: checkSegmentHit uses segmentPointDist to test whether the
    latest swipe segment intersects an enemy's circular hitbox.
  - hitEnemiesRef (Set) prevents a single continuous motion from hitting the
    same enemy more than once per frame.
  - Combo System: Successive hits within 1.4 s increase comboCount.
    Damage multiplier = 1 + min(combo × 0.12, 0.6).

--- 4.2 Parry and Counter System ---

  Enemies "charge" an attack (red ring + progress bar) for ~1100–1400 ms.
  At 320 ms into the charge, parryWindowOpen = true.
  Player sees a "⚠ ATTACK!" warning and a red ring on their pet.

  SUCCESS PARRY — player taps their pet during the parry window:
    → Incoming damage nullified.
    → "BLOCK!" animation plays.
    → Counter State activates: next 3 hits deal 2× damage.

  FAILED PARRY — charge completes before player taps:
    → Pet takes damage reduced by its DEF stat only.

--- 4.3 Mana and Special Skills ---

  Mana fills passively over time, +14 on being hit, +13–22 on landing slashes
  (higher with combos/crits).
  At 100 mana the pet's aura shifts to its rarity colour.
  Tapping the pet fires fireSkill().

  Skill types:
    LAZER / POISON   — High single-target damage or damage-over-time.
    BUBBLE           — AoE damage to all enemies on screen.
    HEAL SELF/PARTY  — Restores HP based on ATK × skillHealPercent.

--- 4.4 Enemy Waves and Boss Encounters ---

  Encounters are fetched from /api/explore/:locationId/encounter.
  BattleArena iterates through the returned enemy array via waveIndex,
  showing a "Wave Complete" phase between each wave.

  BOSSES:
    - Larger hitbox (radius 24 vs 17 for normal enemies).
    - Rage State triggers at ≤50% HP: erratic movement, red screen tint,
      reduced charge time (1100 ms → 980 ms).
    - Hits cause screen shake and shockwave ring effects.

--- 4.5 PvP Mechanics ---

  AI pets wander toward the nearest player pet.
  Damage comes from collision (not from parry/charge).
  Skill targeting requires tapping the specific opponent pet (e.g., Lazer).

--- 4.6 Rewards ---

  On enemy defeat → POST /api/explore/defeat/:enemyId
  Server calculates XP (added to active pet's levelPoints), coins, and
  possible item drops defined in the enemyDrops table.

================================================================================
SECTION 5 — PET SYSTEM OVERVIEW
================================================================================

FILES:
  shared/schema.ts                        — shopItems, userInventory,
                                            petEquippedAccessories,
                                            petHousePositions, petTemplates,
                                            petTemplateParts
  server/routes.ts                        — all /api/pet/... endpoints
  client/src/components/PetDetailPage.tsx
  client/src/components/PetAnimator.tsx
  client/src/pages/PetHousePage.tsx
  client/src/pages/PetWorldPage.tsx

--- 5.1 Pet Stats ---

  Stored in user_inventory per owned pet.
  petHealth  — base 1000
  petAtk     — base 50
  petDef     — base 50
  petLevel   — 1 to 100
  petLevelPoints — XP toward next level
  Rarity (1–5★) — defined in shop_items; controls power-up limits.

--- 5.2 Hatching ---

  Purchase sets hatchStartedAt to current time.
  hatchTime (hours) is defined on the shop_item.
  GET /api/pet/:inventoryId/hatch-check compares elapsed time to hatchTime.
  If complete → isHatched = true.
  Special items with specialType "hatch_time" shift hatchStartedAt backward
  to accelerate hatching.

--- 5.3 Leveling and Power-Ups ---

  XP formula: 100 + level × 30 + level² × 5

  Power-up slots per level = (Rarity + 2) × Level
  Once that many items have been used this level, no more stat items until
  the next level-up (resets itemsUsedThisLevel counter).

  Stat-boosting items target: atk, def, health, or lvl (direct XP grant).
  POST /api/pet/:inventoryId/power-up

  Reset for 300 coins → Level 1, base stats, accessories removed.
  POST /api/pet/:inventoryId/reset-stats

--- 5.4 Accessories ---

  Pets have 3 equip slots.
  POST /api/pet/:inventoryId/equip — adds atkBoost, defBoost, or healthBoost
    from the accessory's shop_item definition.
  POST /api/pet/:inventoryId/unequip — reverses those bonuses.
  Tracked in pet_equipped_accessories (junction table).

  Nicknames (up to 20 chars) — PATCH /api/inventory/:inventoryId/nickname.

--- 5.5 Pet Templates and Visuals ---

  pet_templates + pet_template_parts tables.
  Each template is assembled from named parts (Body, Eyes, Mouth, Wings, Tail)
  with posX, posY, zIndex offsets.
  PetAnimator.tsx renders idle/hover states.
  In PetWorldPage: canFly = true → floating animation; false → ground drift.

--- 5.6 Pet House ---

  Players drag-and-drop pets within their house.
  Positions (posLeft, posTop) saved to pet_house_positions.
  PATCH /api/pet-house-positions/:inventoryId
  Other players can visit to view the collection.

================================================================================
SECTION 6 — ITEMS AND INVENTORY
================================================================================

FILES:
  shared/schema.ts      — shopItems, userInventory, playerFishingEquipment,
                          playerFishInventory, petEquippedAccessories,
                          playerMarketListings
  server/routes.ts      — all item/inventory endpoints
  client/src/components/PetInventory.tsx
  client/src/components/PetDetailPage.tsx

--- 6.1 Item Types (shop_items.type field) ---

  pet          — Eggs / hatched pets.
  power_up     — Consumable stat boosters (atk, def, health, lvl).
  item         — General consumable or resource.
  special      — Special effect (e.g., hatch accelerator).
  accessory    — Equippable passive gear for pets.
  fishing      — Fishing gear (sub-typed by fishingType: pole | bait | fish).
  potion       — In-battle consumables (Healing, Mana, Revive).

--- 6.2 Inventory Tables ---

  user_inventory          — Core ownership table: userId, itemId, quantity,
                            all pet stats (health, atk, def, level, xp),
                            isHatched, hatchStartedAt, nickname, marketListed.
  player_fishing_equipment — Tracks currently active pole and bait per user.
  player_fish_inventory   — Records caught fish per player.
  pet_equipped_accessories — Junction: links accessory inventoryId → pet inventoryId.
  player_market_listings  — Active listings on the Mire Bazaar.

--- 6.3 Key Inventory Rules ---

  - Pole durability decreases every cast (not just on success).
  - Bait is consumed only on a successful catch.
  - Accessories cannot be equipped if all 3 slots are full; the player must
    unequip one first.
  - Potions are used during battle from a dedicated hotbar (not the main UI).
  - A pet can be listed on the market while still in the player's house; it
    becomes unavailable for battle while listed.

================================================================================
SECTION 7 — ADMIN SYSTEM
================================================================================

FILES:
  client/src/pages/AdminPage.tsx              — main admin hub
  client/src/components/ItemDatabaseSection.tsx
  client/src/components/FishingAdminPanel.tsx
  client/src/components/PetDatabasePanel.tsx
  server/routes.ts                            — all /api/admin/... routes

--- 7.1 Access Control ---

  Server: isAdmin middleware checks req.user.isAdmin before every admin route.
  Client: /admin route in App.tsx is conditionally rendered based on user.isAdmin.

--- 7.2 Admin Sections ---

  MEMBERS      — Search users, ban/unban, reset passwords, adjust coin balances.
                 Route: POST /api/admin/coins/:userId

  REWARDS      — Create Reward Bundles (coins + items) and broadcast to all
                 users or specific users.
                 Route: POST /api/admin/reward-bundle

  ITEM DB      — View/create/edit all items and pets in the game database.
                 Component: ItemDatabaseSection.tsx
                 Route (create): POST /api/admin/shop

  ANIMATIONS   — Assemble pet visual templates from body parts.
                 Component: PetDatabasePanel.tsx
                 Routes: POST /api/admin/pet-templates,
                         POST /api/admin/pet-templates/:id/assemble

  FISHING      — Manage fish items and their visual parts.
                 Component: FishingAdminPanel.tsx
                 Routes: POST /api/admin/fish-parts/:fishItemId,
                         PATCH /api/admin/fish-parts/:partId

  SUPPORT      — View and manage player support messages.
                 Route: GET /api/admin/support-messages

================================================================================
SECTION 8 — HOW FISH AND ITEMS ARE ADDED FROM THE ADMIN PANEL
================================================================================

--- 8.1 Adding a Regular Item ---

  1. Open Admin → Item DB section.
  2. Choose item type (power_up, accessory, potion, special, etc.).
  3. Fill in: name, description, price, rarity (1–5), stat boosts.
  4. Upload image → server processes it with Sharp for consistent sizing.
  5. Item appears in the global shop_items database.
  6. To make it available in a world region: assign it via the Map/World editor.
     Route: POST /api/admin/location/:locationId/assign-item/:itemId

--- 8.2 Adding a Pet ---

  STEP 1 — Build the Pet Template (Admin → Animation Parts):
    a. Create a new PetTemplate record.
    b. Upload individual body parts (eyes, body, tail, wings, etc.) for both
       front and back views, with posX/posY/zIndex for each.
    c. Trigger assembly: POST /api/admin/pet-templates/:id/assemble
       This composites parts into a preview image.

  STEP 2 — Create the Pet Item (Admin → Item DB → Pets):
    a. Create a new item with type "pet".
    b. Link it to the PetTemplate created in Step 1.
    c. Upload Egg image and Hatched image.
    d. Set hatchTime (hours), base stats, rarity.
    e. Assign to a world shop as in 8.1 step 6.

--- 8.3 Adding a Fish ---

  1. Open Admin → Fishing section (FishingAdminPanel.tsx).
  2. Create a new item with type "fishing" and fishingType "fish".
  3. Set: name, rarity, sell price, weight in the pool.
  4. Upload Fish Parts (Body, Eyes, Tail) individually via
     POST /api/admin/fish-parts/:fishItemId
  5. Position each part on the coordinate system via
     PATCH /api/admin/fish-parts/:partId
     The minigame uses these coordinates to render the fish dynamically.
  6. Assign the fish to a fishing location/pond so it appears in catches.

--- 8.4 Adding a Fishing Pole or Bait ---

  Same flow as a regular item (8.1) but:
  - type = "fishing", fishingType = "pole" or "bait".
  - Set pole-specific fields: poleMaxUses, poleSlowdown3/4/5,
    rareCatchBoostPercent.
  - Set bait-specific fields: baitCatchBoost, baitRarityBoostStar,
    rarityBoostPercent.

================================================================================
SECTION 9 — FILE STRUCTURE OVERVIEW
================================================================================

/
├── client/                      Frontend (React + Vite + TypeScript)
│   ├── index.html
│   ├── public/                  Static assets (icons, manifest)
│   └── src/
│       ├── App.tsx              Route definitions (wouter)
│       ├── main.tsx             Entry point
│       ├── assets/              Game images and icons (@assets/... alias)
│       ├── components/          Reusable components
│       │   ├── ui/              shadcn/ui base components
│       │   ├── BattleArena.tsx  PvE combat engine
│       │   ├── PetAnimator.tsx  Pet visual renderer
│       │   ├── PetDetailPage.tsx
│       │   ├── PetInventory.tsx
│       │   ├── ItemDatabaseSection.tsx  Admin: items & pets
│       │   ├── FishingAdminPanel.tsx    Admin: fish management
│       │   └── PetDatabasePanel.tsx     Admin: pet templates
│       ├── hooks/               Custom React hooks (use-toast, use-mobile)
│       ├── lib/
│       │   ├── queryClient.ts   TanStack Query setup + apiRequest helper
│       │   └── sounds.ts        Sound effect manager
│       └── pages/
│           ├── AdminPage.tsx
│           ├── FishingPage.tsx
│           ├── PetHousePage.tsx
│           ├── PetWorldPage.tsx
│           ├── MarketPage.tsx
│           ├── CoinShopPage.tsx
│           ├── PvpArenaPage.tsx
│           ├── PvpBattlePage.tsx
│           └── ... (other region/world pages)
│
├── server/
│   ├── index.ts             Express server setup, middleware, session, passport
│   ├── routes.ts            ALL API endpoints (auth, game, admin)
│   ├── storage.ts           IStorage interface + database implementation
│   ├── db.ts                Drizzle database connection
│   ├── stripeClient.ts      Stripe SDK setup
│   └── webhookHandlers.ts   Stripe webhook processing
│
├── shared/
│   └── schema.ts            Single source of truth: Drizzle schema + Zod types
│
├── attached_assets/         Game art (PNG/JPEG): fish, pets, items, backgrounds
├── drizzle.config.ts        Drizzle ORM config (do NOT modify)
├── vite.config.ts           Vite build config (do NOT modify)
├── package.json             Dependencies and npm scripts (do NOT modify)
└── replit.md                Project notes for the Replit AI environment

================================================================================
SECTION 10 — IMPORTANT GAME MECHANICS SUMMARY
================================================================================

COINS
  - Primary currency. Earned from fishing sales, enemy drops, and market sales.
  - Stored as integer on the user record.

RARITY SYSTEM (1–5★)
  - Applies to fish, pets, accessories, and drops.
  - Higher rarity → better stats, harder to obtain, more power-up slots per level.

WORLD / LOCATION SYSTEM
  - Veridia is divided into themed regions (Swamp, Volcanic, Sky Realm, etc.).
  - Each location has its own: shop items, enemies, fishing pond, and NPCs.
  - The admin assigns items and enemies to specific locations.

MARKET (MIRE BAZAAR)
  - Fully player-to-player. Items listed in player_market_listings.
  - Pets listed on market are unavailable for battle until unlisted.

AQUARIUM
  - Players can display caught fish in a personal aquarium instead of selling.

SUPPORT SYSTEM
  - Players can submit support tickets from within the game.
  - Admin panel has an inbox to view and respond.

SOUND SYSTEM
  - lib/sounds.ts manages all in-game audio.
  - Sounds are triggered at key game events (catch, battle hit, level-up, etc.).

AUTHENTICATION
  - Passport.js handles sessions.
  - User record stores: username, password hash, coins, isAdmin flag, activePetId.

STRIPE PAYMENTS
  - Used for premium currency or shop purchases.
  - Webhooks handled in server/webhookHandlers.ts.

================================================================================
SECTION 11 — WHAT AN AI SHOULD KNOW BEFORE EDITING CODE
================================================================================

1. SCHEMA IS THE SOURCE OF TRUTH
   shared/schema.ts defines every database table, column, and type.
   Always check this file first before adding new fields or endpoints.
   Use createInsertSchema from drizzle-zod to derive insert types.

2. STORAGE LAYER
   All database reads/writes go through server/storage.ts (IStorage interface).
   Do NOT write raw SQL in routes.ts — add a method to IStorage and implement
   it in the storage class, then call it from the route.

3. FRONTEND DATA FETCHING
   Uses TanStack Query v5 (object form only, no positional args).
   Default fetcher is already configured; queries just need a queryKey.
   Mutations use apiRequest from @lib/queryClient.
   Always invalidate affected queryKeys after mutations.

4. ROUTING
   Frontend: wouter (Link, useLocation). Never use window.location directly.
   Backend: all game routes in server/routes.ts, prefix /api/.
   Admin routes are protected by isAdmin middleware on the server AND
   conditionally rendered on the client.

5. DO NOT MODIFY THESE FILES without a very good reason:
   - vite.config.ts
   - drizzle.config.ts
   - package.json (use package manager tools to add dependencies instead)

6. ENVIRONMENT / IMPORTS
   - React is auto-imported by the JSX transformer. Do NOT import React manually.
   - Use import.meta.env.VITE_* for frontend env vars (not process.env).
   - Use @assets/... for game images in the frontend.
   - Use @/ prefix for src/ aliases (components, hooks, lib, pages).

7. FISHING MINIGAME STATE MACHINE
   The fishing page is a complex state machine. When editing it, trace the
   full phase flow (IDLE → CASTING → WAITING → NIBBLE → REELING → CAUGHT/MISSED)
   and ensure state transitions are not broken.

8. BATTLE SYSTEM IS FRAME-BASED
   BattleArena.tsx uses requestAnimationFrame loops. Be careful when adding
   state updates inside these loops — ensure you are using refs where needed
   to avoid stale closure bugs.

9. PET TEMPLATE ASSEMBLY
   Pet visuals depend on the pet_template_parts coordinate system. If you add
   new pets, they MUST be assembled through the admin panel, not hard-coded.

10. IMAGE UPLOADS
    The server uses Sharp to process uploaded images. Keep this pipeline intact
    when adding new upload endpoints.

11. ADDING NEW ITEMS OR FISH
    Always use the Admin Panel flow described in Section 8. Do not seed items
    directly in SQL without also updating the admin UI to manage them.

12. POWER-UP SLOT FORMULA
    power-up slots = (rarity + 2) × level
    This controls how many stat items a pet can consume per level. Any changes
    to rarity or leveling must re-check this formula.

13. SERVER-SIDE CATCH VALIDATION
    The fishing minigame is client-side but the final catch decision is server-
    side. The client sends a performanceScore; the server does the final roll.
    Never bypass this by trusting only client state.

14. DATA-TESTID ATTRIBUTES
    All interactive and meaningful display elements should have data-testid
    attributes following the pattern: {action}-{target} or {type}-{content}.
    Dynamic lists: {type}-{description}-{id}.

================================================================================
SECTION 12 — DATABASE STRUCTURE
================================================================================

Source of truth: shared/schema.ts
ORM: Drizzle ORM with PostgreSQL
All IDs are UUID strings generated by gen_random_uuid() unless noted.
All timestamps default to now() unless noted.

This section documents every table, every column, what it means, and how
tables relate to each other. Read this before writing any new query or
migration.

────────────────────────────────────────────────────────────────────────────────
TABLE: users
Purpose: One row per registered player account.
────────────────────────────────────────────────────────────────────────────────

  id                    UUID PK         Unique player identifier.
  username              text UNIQUE     Display name (3–20 chars, alphanumeric + underscore).
  email                 text UNIQUE     Login email address.
  password              text            Bcrypt-hashed password. Never stored plain.
  profile_image         text NULL       URL of the player's avatar image.
  coins                 integer         Current coin balance. Never goes negative.
  is_admin              boolean         TRUE grants access to all /api/admin/* routes
                                        and renders the admin panel on the client.
  is_banned             boolean         TRUE blocks login.
  active_pet_id         varchar NULL    FK → user_inventory.id (the pet currently
                                        displayed/used in battle).
  last_username_change  timestamp NULL  Rate-limits username changes.
  last_profile_pic_change timestamp NULL Rate-limits avatar changes.
  password_reset_token  text NULL       One-time token for password reset flow.
  password_reset_expires timestamp NULL Expiry for the reset token.
  market_extra_slots    integer         Extra market listing slots purchased (default 0).
  welcome_v2_sent       boolean         Tracks whether the welcome reward bundle was
                                        already delivered (prevents double-send).
  created_at            timestamp       Account creation time.

RELATIONSHIPS:
  users.id ← user_inventory.user_id         (player owns inventory items)
  users.id ← player_fish_inventory.user_id  (player owns caught fish)
  users.id ← player_fishing_equipment.user_id (active fishing gear)
  users.id ← player_market_listings.seller_id / buyer_id
  users.id ← friendships.requester_id / receiver_id
  users.id ← notifications.user_id
  users.id ← pvp_battles.user_id
  users.id ← pvp_battle_groups.user_id
  users.id ← pet_house_positions.user_id
  users.id ← world_pet_positions.owner_user_id
  users.id ← user_rewards.user_id
  users.id ← user_badges.user_id
  users.id ← badge_reward_claims.user_id
  users.id ← coin_purchases.user_id
  users.active_pet_id → user_inventory.id

────────────────────────────────────────────────────────────────────────────────
TABLE: shop_items
Purpose: Master catalogue of every item in the game — pets, accessories,
         fishing gear, fish, potions, power-ups, and special items.
         This is NOT per-player; it is the global item definition table.
────────────────────────────────────────────────────────────────────────────────

  id                    UUID PK
  name                  text            Display name.
  price                 integer         Shop purchase price in coins.
  type                  text            Item category. Valid values:
                                          "pet"       — hatching egg / pet
                                          "power_up"  — stat booster consumable
                                          "item"      — general consumable/resource
                                          "special"   — special-effect item
                                          "accessory" — equippable pet gear
                                          "fishing"   — fishing gear or fish
                                          "potion"    — in-battle consumable
  world_id              text            Which world this item belongs to.
  location_id           varchar NULL    FK → world_locations.id — which shop
                                        sells this item (NULL = not in any shop).
  image_url             text NULL       Main display image URL.
  rarity                integer NULL    1–5 star rarity for most items.

  --- PET-SPECIFIC FIELDS ---
  hatch_time            integer NULL    Time in hours for egg to hatch.
  egg_image_url         text NULL       Image shown while egg is unhatched.
  hatched_image_url     text NULL       Image shown after hatching.
  pet_template_id       varchar NULL    FK → pet_templates.id — links to the
                                        visual part assembly for this pet.
  special_skill         text NULL       Name of the pet's special skill
                                        (e.g., "Heal", "Poison", "Lazer").
  special_skill_type    text NULL       Skill behaviour type used by BattleArena
                                        to determine skill logic.
  skill_damage_percent  integer NULL    % of ATK used as damage for attack skills.
  skill_heal_percent    integer NULL    % of ATK used as healing for heal skills.

  --- STAT BOOST FIELDS (power_up / item) ---
  stat_boost_type       text NULL       Which stat is boosted: "atk", "def",
                                        "health", or "lvl" (direct XP grant).
  stat_boost_amount     integer NULL    How much to add to the target stat.

  --- POTION FIELDS ---
  health_restored       integer NULL    HP restored when this potion is used.
  mana_restored         integer NULL    Mana restored when this potion is used.
  pets_revived          integer NULL    Number of KO'd pets revived (revive potions).

  --- ACCESSORY FIELDS ---
  atk_boost             integer NULL    Flat ATK added when this accessory is equipped.
  def_boost             integer NULL    Flat DEF added when this accessory is equipped.
  health_boost          integer NULL    Flat HP added when this accessory is equipped.

  --- SPECIAL ITEM FIELDS ---
  special_type          text NULL       What the special item does. Known value:
                                          "hatch_time" — accelerates egg hatching.
  special_amount        integer NULL    Magnitude of the special effect.

  --- SHOP VISUAL PLACEMENT ---
  shop_pos_x            real            X% position within the shop scene (default 50).
  shop_pos_y            real            Y% position within the shop scene (default 50).
  shop_width            integer         Display width in the shop scene (default 72).

  --- FISHING GEAR FIELDS ---
  fishing_type          text NULL       Sub-type for type="fishing" items:
                                          "pole" | "bait" | "fish"
  hookless_image_url    text NULL       Alt image of the pole without hook (UI use).
  pole_max_uses         integer NULL    Total casts before the pole breaks.
  pole_slowdown_3       real NULL       Tension rise multiplier for 3★ fish (< 1.0
                                        = easier; e.g., 0.8 = 20% slower tension).
  pole_slowdown_4       real NULL       Same, for 4★ fish.
  pole_slowdown_5       real NULL       Same, for 5★ fish.
  rare_catch_boost_percent integer NULL Added weight % applied to 4★+5★ fish
                                        in the catch pool when using this pole.

  --- BAIT FIELDS ---
  bait_catch_boost      integer NULL    Bonus progress gain per tick during reeling.
  bait_rarity_boost_star integer NULL  Which star tier (1–5) this bait targets.
  rarity_boost_percent  integer NULL    Extra weight % added to the targeted rarity tier.

  --- FISH FIELDS ---
  star_rarity           integer NULL    Fish-specific rarity (1–5★) used in pool
                                        weighting. Separate from the general "rarity"
                                        field to avoid conflicts.
  facing_direction      text NULL       "left" or "right" — which way the fish
                                        sprite faces in the minigame.
  fish_swim_zone        text NULL       Visual zone in the pond where the fish appears
                                        (e.g., "surface", "mid", "bottom").

  created_at            timestamp

RELATIONSHIPS:
  shop_items.id ← user_inventory.shop_item_id       (player owns this item)
  shop_items.id ← pond_fish.shop_item_id            (fish in a pond)
  shop_items.id ← player_fish_inventory.shop_item_id (caught fish log)
  shop_items.id ← player_fish_catch_log.shop_item_id (first-catch log)
  shop_items.id ← enemy_drops.shop_item_id          (enemy loot table)
  shop_items.id ← reward_bundle_items.shop_item_id  (admin bundles)
  shop_items.id ← player_market_listings.shop_item_id
  shop_items.id ← fish_template_parts.fish_item_id  (fish visual parts)
  shop_items.pet_template_id → pet_templates.id

────────────────────────────────────────────────────────────────────────────────
TABLE: user_inventory
Purpose: Every item a player currently owns. One row per owned item instance
         (not per item type — a player with 3 health potions has quantity=3
         on a single row, but a player's two different pets have two rows).
────────────────────────────────────────────────────────────────────────────────

  id                    UUID PK         Inventory instance ID.
  user_id               varchar         FK → users.id.
  shop_item_id          varchar         FK → shop_items.id — what item this is.
  acquired_at           timestamp       When the player obtained this item.
  quantity              integer         Stack count (mostly for stackable items
                                        like potions/bait; always 1 for pets).

  --- PET-SPECIFIC FIELDS (only meaningful when shop_items.type = "pet") ---
  hatch_started_at      timestamp NULL  When hatching began. NULL = not yet started.
  is_hatched            boolean         FALSE = still an egg. TRUE = can battle.
  pet_health            integer         Current HP (base 1000). Depletes in battle.
  pet_atk               integer         Current ATK stat (base 50).
  pet_def               integer         Current DEF stat (base 50).
  pet_level             integer         Current level (1–100).
  pet_level_points      integer         Accumulated XP toward the next level.
  items_used_this_level integer         Count of power-up items used since last
                                        level-up. Resets to 0 on each level-up.
                                        Capped at (rarity + 2) × level.
  pet_nickname          text NULL       Custom name set by the player (≤20 chars).

  --- FISHING POLE FIELD ---
  pole_uses_left        integer NULL    Remaining casts for a pole item. NULL for
                                        non-pole items. Decrements every cast.

  --- MARKET FLAG ---
  is_listed             boolean         TRUE = item is actively listed in the
                                        Mire Bazaar and cannot be used in battle
                                        or equipped until unlisted.

RELATIONSHIPS:
  user_inventory.user_id → users.id
  user_inventory.shop_item_id → shop_items.id
  user_inventory.id ← pet_equipped_accessories.pet_inventory_id (as the pet)
  user_inventory.id ← pet_equipped_accessories.accessory_inventory_id (as accessory)
  user_inventory.id ← pet_house_positions.inventory_id
  user_inventory.id ← player_fishing_equipment.pole_inventory_id
  user_inventory.id ← player_fishing_equipment.bait_inventory_id
  user_inventory.id ← player_market_listings.inventory_id
  users.active_pet_id → user_inventory.id

────────────────────────────────────────────────────────────────────────────────
TABLE: pet_templates
Purpose: Defines the visual assembly blueprint for a pet type. Each pet item
         in shop_items points to one template. The template stores metadata;
         actual parts are in pet_template_parts.
────────────────────────────────────────────────────────────────────────────────

  id                    UUID PK
  name                  text            Descriptive name for the admin panel.
  facing                text NULL       Default facing ("front" or "back").
  front_assembled       text NULL       URL of the pre-composited front-facing
                                        image (generated by admin assembly tool).
  back_assembled        text NULL       URL of the pre-composited back-facing image.
  can_fly               boolean         TRUE → floating animation in PetWorldPage.
                                        FALSE → ground drift animation.
  created_at            timestamp

RELATIONSHIPS:
  pet_templates.id ← shop_items.pet_template_id
  pet_templates.id ← pet_template_parts.template_id

────────────────────────────────────────────────────────────────────────────────
TABLE: pet_template_parts
Purpose: Individual visual layers that make up a pet's appearance. Multiple
         rows per template (one per body part). Rendered in zIndex order.
────────────────────────────────────────────────────────────────────────────────

  id                    UUID PK
  template_id           varchar         FK → pet_templates.id.
  part_type             text            Named layer: "Body", "Eyes", "Mouth",
                                        "Wings", "Tail", etc.
  view                  text            "front" or "back" — which direction this
                                        part belongs to.
  image_url             text            URL of this part's PNG image.
  pos_x                 integer         X pixel offset from canvas origin.
  pos_y                 integer         Y pixel offset from canvas origin.
  width                 integer         Render width in pixels.
  height                integer         Render height in pixels.
  z_index               integer         Stacking order (higher = in front).
  pivot_x               integer         Pivot % X for animations (default 50).
  pivot_y               integer         Pivot % Y for animations (default 50).

RELATIONSHIPS:
  pet_template_parts.template_id → pet_templates.id

────────────────────────────────────────────────────────────────────────────────
TABLE: pet_equipped_accessories
Purpose: Junction table tracking which accessories are equipped to which pet.
         Each pet can have up to 3 rows in this table (slots 0, 1, 2).
────────────────────────────────────────────────────────────────────────────────

  id                       UUID PK
  pet_inventory_id         varchar   FK → user_inventory.id (the pet row).
  accessory_inventory_id   varchar   FK → user_inventory.id (the accessory row).
  slot                     integer   Slot number: 0, 1, or 2.
  created_at               timestamp

RULE: When an accessory is equipped, the pet's atk/def/health in user_inventory
      are immediately incremented. When unequipped, they are decremented.
      The stats on user_inventory always reflect the current total including
      all equipped accessories.

RELATIONSHIPS:
  pet_equipped_accessories.pet_inventory_id → user_inventory.id
  pet_equipped_accessories.accessory_inventory_id → user_inventory.id

────────────────────────────────────────────────────────────────────────────────
TABLE: fish_template_parts
Purpose: Visual layers for rendering a fish inside the fishing minigame.
         Similar concept to pet_template_parts but fish-specific.
────────────────────────────────────────────────────────────────────────────────

  id                    UUID PK
  fish_item_id          varchar         FK → shop_items.id (the fish item).
  part_type             text            Layer name: "Body", "Eyes", "Tail", etc.
  image_url             text            URL of the part image.
  pos_x                 integer         X pixel position on the fish canvas.
  pos_y                 integer         Y pixel position on the fish canvas.
  width                 integer         Render width in pixels.
  height                integer         Render height in pixels.
  z_index               integer         Stacking order.

HOW FISH PARTS ARE USED IN THE MINIGAME:
  During the REELING phase, FishingPage.tsx fetches the fish's template parts
  and composites them in real time on an HTML canvas using the pos_x/pos_y
  and z_index values. This creates the animated fish the player sees while
  reeling. The facing_direction on the shop_items row tells the renderer
  which way to mirror the sprite.

RELATIONSHIPS:
  fish_template_parts.fish_item_id → shop_items.id

────────────────────────────────────────────────────────────────────────────────
TABLE: pond_fish
Purpose: Maps which fish species can appear in which fishing location.
         One row per fish-per-location assignment. A fish can be in multiple
         ponds; a pond can have multiple fish.
────────────────────────────────────────────────────────────────────────────────

  id                    UUID PK
  location_id           varchar         FK → world_locations.id.
  shop_item_id          varchar         FK → shop_items.id (must be type "fishing",
                                        fishingType "fish").
  created_at            timestamp

HOW IT IS USED:
  When a player casts, the server queries pond_fish for the current location,
  builds a weighted pool using each fish's star_rarity, applies pole/bait
  modifiers, then performs a weighted random selection to pick which fish
  the player encounters.

RELATIONSHIPS:
  pond_fish.location_id → world_locations.id
  pond_fish.shop_item_id → shop_items.id

────────────────────────────────────────────────────────────────────────────────
TABLE: player_fish_inventory
Purpose: Every fish a player currently owns (caught but not sold).
         Separate from user_inventory because fish have different lifecycle
         rules — they are caught, not purchased.
────────────────────────────────────────────────────────────────────────────────

  id                    UUID PK
  user_id               varchar         FK → users.id.
  shop_item_id          varchar         FK → shop_items.id (the fish species).
  caught_at             timestamp       When this specific fish was caught.
  in_aquarium           boolean         TRUE = displayed in player's aquarium.
                                        FALSE = in inventory (can be sold).

RELATIONSHIPS:
  player_fish_inventory.user_id → users.id
  player_fish_inventory.shop_item_id → shop_items.id

────────────────────────────────────────────────────────────────────────────────
TABLE: player_fish_catch_log
Purpose: Records the FIRST TIME a player catches each species. Used for the
         Fish Book (collection tracker) and first-catch reward claims.
         Only one row per player-per-species (not per individual catch).
────────────────────────────────────────────────────────────────────────────────

  id                    UUID PK
  user_id               varchar         FK → users.id.
  shop_item_id          varchar         FK → shop_items.id.
  first_caught_at       timestamp       When this species was first caught.
  reward_claimed        boolean         Whether the first-catch bonus reward
                                        has been collected.

RELATIONSHIPS:
  player_fish_catch_log.user_id → users.id
  player_fish_catch_log.shop_item_id → shop_items.id

────────────────────────────────────────────────────────────────────────────────
TABLE: player_fishing_equipment
Purpose: Tracks which pole and bait are currently "active" (equipped) for
         each player. One row per player (unique constraint on user_id).
────────────────────────────────────────────────────────────────────────────────

  id                    UUID PK
  user_id               varchar UNIQUE  FK → users.id.
  pole_inventory_id     varchar NULL    FK → user_inventory.id (must be a pole).
                                        NULL = no pole equipped.
  bait_inventory_id     varchar NULL    FK → user_inventory.id (must be bait).
                                        NULL = no bait equipped.
  updated_at            timestamp       Last time equipment was changed.

RELATIONSHIPS:
  player_fishing_equipment.user_id → users.id
  player_fishing_equipment.pole_inventory_id → user_inventory.id
  player_fishing_equipment.bait_inventory_id → user_inventory.id

────────────────────────────────────────────────────────────────────────────────
TABLE: player_market_listings
Purpose: Active and historical listings on the Mire Bazaar (player marketplace).
────────────────────────────────────────────────────────────────────────────────

  id                    UUID PK
  seller_id             varchar         FK → users.id (who listed it).
  seller_name           text            Denormalised seller username (for display).
  inventory_id          varchar         FK → user_inventory.id (the item listed).
  shop_item_id          varchar         FK → shop_items.id.
  item_name             text            Denormalised item name.
  item_image_url        text NULL       Denormalised image URL.
  item_type             text            Denormalised type.
  price                 integer         Asking price in coins.
  status                text            "active" | "sold" | "cancelled"
  buyer_id              varchar NULL    FK → users.id — set when sold.
  created_at            timestamp

RULE: When a listing is created, user_inventory.is_listed is set to TRUE.
      When sold or cancelled, is_listed is set back to FALSE.

RELATIONSHIPS:
  player_market_listings.seller_id → users.id
  player_market_listings.buyer_id → users.id
  player_market_listings.inventory_id → user_inventory.id
  player_market_listings.shop_item_id → shop_items.id

────────────────────────────────────────────────────────────────────────────────
TABLE: location_enemies
Purpose: Defines enemy encounters for a specific world location.
────────────────────────────────────────────────────────────────────────────────

  id                    UUID PK
  location_id           varchar         FK → world_locations.id.
  name                  text            Enemy display name.
  image_url             text NULL       Enemy sprite URL.
  is_boss               boolean         TRUE → boss hitbox, rage state, effects.
  coin_reward           integer         Base coins dropped when defeated.
  sort_order            integer         Wave order within the encounter sequence.
  created_at            timestamp

RELATIONSHIPS:
  location_enemies.location_id → world_locations.id
  location_enemies.id ← enemy_drops.enemy_id

────────────────────────────────────────────────────────────────────────────────
TABLE: enemy_drops
Purpose: Loot table — which items can drop from which enemy and at what rate.
────────────────────────────────────────────────────────────────────────────────

  id                    UUID PK
  enemy_id              varchar         FK → location_enemies.id.
  shop_item_id          varchar         FK → shop_items.id — what can drop.
  drop_rate             integer         Percentage chance (1–100) per kill.
  created_at            timestamp

RELATIONSHIPS:
  enemy_drops.enemy_id → location_enemies.id
  enemy_drops.shop_item_id → shop_items.id

────────────────────────────────────────────────────────────────────────────────
TABLE: world_locations
Purpose: Named places within a world — shops, fishing spots, battle zones, etc.
────────────────────────────────────────────────────────────────────────────────

  id                    UUID PK
  world_id              text            FK → worlds.id.
  name                  text            Location display name.
  type                  text            Location category (e.g., "shop", "fishing",
                                        "explore", "hub").
  icon_url              text NULL       Map node icon image.
  bg_url                text NULL       Background image shown when inside location.
  description           text NULL       Flavour text.
  pos_x                 real            X% on the world map (0–100).
  pos_y                 real            Y% on the world map (0–100).
  flipped               boolean         Mirror the icon horizontally.
  shopkeeper_id         varchar NULL    Internal ID for the shopkeeper NPC.
  shopkeeper_name       text NULL       NPC display name.
  shopkeeper_image_url  text NULL       NPC portrait.
  owner_image_url       text NULL       Owner/extra character image.
  is_shop               boolean         TRUE = shop tab is shown at this location.
  glow_color            text NULL       CSS colour for the map node glow effect.
  sort_order            integer         Display order on the map.
  icon_size             integer         Icon pixel size on the world map.
  created_at            timestamp

RELATIONSHIPS:
  world_locations.world_id → worlds.id
  world_locations.id ← shop_items.location_id
  world_locations.id ← pond_fish.location_id
  world_locations.id ← location_enemies.location_id
  world_locations.id ← location_objects.location_id

────────────────────────────────────────────────────────────────────────────────
TABLE: worlds
Purpose: Top-level world definitions. Each world contains multiple locations.
────────────────────────────────────────────────────────────────────────────────

  id                    varchar PK      Manually assigned slug (e.g., "veridia").
  name                  text            World display name.
  icon_url              text NULL       Thumbnail icon.
  bg_url                text NULL       World map background image.
  sky_image_url         text NULL       Sky layer image for parallax effect.
  ground_image_url      text NULL       Ground layer image for parallax effect.
  pos_x                 integer         X position on the global map (if applicable).
  pos_y                 integer         Y position on the global map (if applicable).
  icon_size             integer         Icon size on the world select screen.
  glow_color            text            CSS colour for the world icon glow.
  is_default            boolean         TRUE = players start here by default.
  created_at            timestamp

RELATIONSHIPS:
  worlds.id ← world_locations.world_id
  worlds.id ← world_buildings.world_id
  worlds.id ← world_decor_items.world_id
  worlds.id ← world_decor_placements.world_id
  worlds.id ← world_pet_positions.world_id
  worlds.id ← fish_barrels.world_id

────────────────────────────────────────────────────────────────────────────────
TABLE: world_buildings
Purpose: Decorative or navigational buildings placed in a world hub scene.
────────────────────────────────────────────────────────────────────────────────

  id                        UUID PK
  world_id                  text       FK → worlds.id.
  name                      text       Building label.
  image_url                 text NULL  Building image.
  side                      text       "left" or "right" — which side of the hub.
  pos_y                     integer    Y% position in the hub scene.
  destination_page          text NULL  React route to navigate to on click.
  destination_location_id   varchar NULL FK → world_locations.id (if clicking
                                          sends player to a location).
  sort_order                integer    Display order.
  created_at                timestamp

────────────────────────────────────────────────────────────────────────────────
TABLE: location_objects
Purpose: Decorative foreground/background objects placed within a location scene.
────────────────────────────────────────────────────────────────────────────────

  id                    UUID PK
  location_id           varchar         FK → world_locations.id.
  image_url             text            Object image.
  pos_x                 integer         X% in the scene.
  pos_y                 integer         Y% in the scene.
  width                 integer         Display width.
  created_at            timestamp

────────────────────────────────────────────────────────────────────────────────
TABLE: world_decor_items   &   world_decor_placements
Purpose: Admin-managed decorative items and their placed instances in worlds.
────────────────────────────────────────────────────────────────────────────────

  world_decor_items:
    id, world_id, name, image_url, created_at

  world_decor_placements:
    id, world_id, decor_item_id (FK → world_decor_items.id),
    name, image_url (denormalised), pos_x (%), pos_y (%), size, flipped,
    message (optional tooltip/sign text), created_at

────────────────────────────────────────────────────────────────────────────────
TABLE: fish_barrels
Purpose: Interactive barrel objects placed in the world. Players can click
         them to sell fish directly from the world scene.
────────────────────────────────────────────────────────────────────────────────

  id                    UUID PK
  world_id              text            FK → worlds.id.
  pos_x                 real            X% in the world scene.
  pos_y                 real            Y% in the world scene.
  size                  integer         Display size in pixels.
  created_at            timestamp

────────────────────────────────────────────────────────────────────────────────
TABLE: pet_house_positions
Purpose: Stores where each pet is placed within a player's Pet House.
         Unique constraint on (user_id, inventory_id) — one position per pet
         per player.
────────────────────────────────────────────────────────────────────────────────

  id                    UUID PK
  user_id               varchar         FK → users.id.
  inventory_id          varchar         FK → user_inventory.id (the pet instance).
  pos_left              text            CSS left value (e.g., "35%") of pet placement.
  pos_top               text            CSS top value of pet placement.
  updated_at            timestamp

UNIQUE INDEX: (user_id, inventory_id)

────────────────────────────────────────────────────────────────────────────────
TABLE: world_pet_positions
Purpose: Where each player's active pet visually appears on the shared world
         map scene. One row per player per world.
────────────────────────────────────────────────────────────────────────────────

  id                    UUID PK
  world_id              text            FK → worlds.id.
  owner_user_id         varchar         FK → users.id.
  pos_x                 real            X% position on the world scene.
  pos_y                 real            Y% position on the world scene.
  updated_at            timestamp

UNIQUE INDEX: (world_id, owner_user_id)

────────────────────────────────────────────────────────────────────────────────
TABLE: pvp_battles
Purpose: Historical log of every PvP match a player has fought.
────────────────────────────────────────────────────────────────────────────────

  id                    UUID PK
  user_id               varchar         FK → users.id.
  opponent_name         text            Display name of the opponent.
  opponent_image_url    text NULL       Opponent avatar.
  opponent_level        integer         Opponent's level at time of battle.
  opponent_skill        text NULL       Opponent's active skill name.
  result                text            "win" | "loss"
  coins_earned          integer         Coins won/lost from this battle.
  battle_points_delta   integer         Ranking points change (positive or negative).
  created_at            timestamp

────────────────────────────────────────────────────────────────────────────────
TABLE: pvp_battle_groups
Purpose: Each player's selected team of pets for PvP battles. One row per
         player (unique on user_id).
────────────────────────────────────────────────────────────────────────────────

  id                    UUID PK
  user_id               varchar UNIQUE  FK → users.id.
  pet_inventory_ids     text[]          Array of user_inventory.id strings
                                        representing the player's PvP team.
  updated_at            timestamp

────────────────────────────────────────────────────────────────────────────────
TABLE: friendships
Purpose: Tracks friend relationships between players.
────────────────────────────────────────────────────────────────────────────────

  id                    UUID PK
  requester_id          varchar         FK → users.id (who sent the request).
  receiver_id           varchar         FK → users.id (who received it).
  status                text            "pending" | "accepted"
  created_at            timestamp

────────────────────────────────────────────────────────────────────────────────
TABLE: notifications
Purpose: In-game notification inbox per player.
────────────────────────────────────────────────────────────────────────────────

  id                    UUID PK
  user_id               varchar         FK → users.id.
  type                  text            Notification category (e.g., "friend_request",
                                        "market_sale", "reward").
  message               text            Human-readable notification body.
  is_read               boolean         FALSE = unread (shows badge on UI).
  created_at            timestamp

────────────────────────────────────────────────────────────────────────────────
TABLE: badges  &  user_badges  &  badge_reward_claims
Purpose: Achievement badges awarded to players, with optional daily coin rewards.
────────────────────────────────────────────────────────────────────────────────

  badges:
    id, name, image_url,
    daily_reward_coins (NULL = no daily reward; integer = coins per daily claim),
    created_at

  user_badges:
    id, user_id (FK → users.id), badge_id (FK → badges.id),
    awarded_at

  badge_reward_claims:
    id, user_id (FK → users.id), badge_id (FK → badges.id),
    last_claimed_at (used to enforce one-claim-per-day)

────────────────────────────────────────────────────────────────────────────────
TABLE: reward_bundles  &  reward_bundle_items  &  user_rewards
Purpose: Admin-created reward packages sent to players.
────────────────────────────────────────────────────────────────────────────────

  reward_bundles:
    id, name, message (optional note shown to player),
    coin_amount (flat coin bonus in this bundle), created_at

  reward_bundle_items:
    id, bundle_id (FK → reward_bundles.id),
    shop_item_id (FK → shop_items.id — item included in the bundle)

  user_rewards:
    id, user_id (FK → users.id), bundle_id (FK → reward_bundles.id),
    claimed (FALSE = pending, TRUE = player collected it), created_at

────────────────────────────────────────────────────────────────────────────────
TABLE: coin_purchases
Purpose: Stripe payment transaction log for real-money coin purchases.
────────────────────────────────────────────────────────────────────────────────

  id                    UUID PK
  user_id               varchar         FK → users.id.
  amount_usd            integer         Amount charged in US cents.
  coins_received        integer         Coins credited to the player.
  stripe_session_id     text            Stripe Checkout session ID (for reconciliation).
  created_at            timestamp

────────────────────────────────────────────────────────────────────────────────
TABLE: support_messages
Purpose: Player-submitted support tickets readable in the admin panel.
────────────────────────────────────────────────────────────────────────────────

  id                    UUID PK
  username              text            Player's username at time of submission.
  email                 text            Contact email.
  subject               text            Ticket subject line.
  message               text            Full message body.
  is_read               boolean         FALSE = unread in admin inbox.
  created_at            timestamp

────────────────────────────────────────────────────────────────────────────────
TABLE: game_settings
Purpose: Key-value store for global game configuration flags. Edited by admin.
────────────────────────────────────────────────────────────────────────────────

  key                   varchar PK      Setting name (e.g., "maintenance_mode").
  value                 text            Setting value (always stored as string;
                                        parse as needed in code).

================================================================================
SECTION 13 — RARITY / STAR SYSTEM
================================================================================

Rarity is a 1–5 integer used across multiple tables. Its meaning depends
on the item type:

PETS (shop_items.rarity):
  1★ — Common.   Power-up slots per level = 3  × level.
  2★ — Uncommon. Power-up slots per level = 4  × level.
  3★ — Rare.     Power-up slots per level = 5  × level.
  4★ — Epic.     Power-up slots per level = 6  × level.
  5★ — Legendary. Power-up slots per level = 7 × level.

FISH (shop_items.star_rarity — separate field):
  Base catch pool weights: 1★:60  2★:24  3★:10  4★:4  5★:2
  Higher-star fish have shorter nibble windows and fewer nibble chances.
  5★ fish: 1.1 s window, 1 chance.
  1★ fish: 2.5 s window, 3 chances.

ACCESSORIES / ITEMS (shop_items.rarity):
  Higher rarity generally indicates better stat boosts; used for display only
  (coloured stars/borders in the UI). No gameplay formula beyond visuals.

ENEMIES / DROPS:
  Rarity is not a field on enemies; rareness is controlled by drop_rate (%)
  in enemy_drops. Lower drop_rate = rarer drop.

================================================================================
SECTION 14 — HOW FISH FLOW THROUGH THE DATABASE
================================================================================

This traces the full lifecycle of a fish from admin creation to player catch.

1. ADMIN CREATES THE FISH
   POST /api/admin/shop  { type: "fishing", fishingType: "fish", star_rarity: N, ... }
   → Row created in shop_items.

2. ADMIN UPLOADS VISUAL PARTS
   POST /api/admin/fish-parts/:fishItemId  { partType: "Body", image: ... }
   → Row created in fish_template_parts linked to the shop_items.id.
   PATCH /api/admin/fish-parts/:partId  { posX, posY, width, height, zIndex }
   → Positions updated so the minigame knows how to render the fish.

3. ADMIN ASSIGNS FISH TO A POND
   POST /api/admin/location/:locationId/assign-fish/:shopItemId
   → Row created in pond_fish linking location_id + shop_item_id.

4. PLAYER CASTS AT THAT LOCATION
   POST /api/fishing/cast  { locationId, poleInventoryId, baitInventoryId }
   Server queries pond_fish for the location, builds weighted pool, selects fish.
   Returns fish data + fish_template_parts so client can render it.
   Decrements user_inventory.pole_uses_left for the pole.

5. PLAYER COMPLETES THE MINIGAME
   POST /api/fishing/catch  { shopItemId, performanceScore, locationId }
   Server validates performance score, performs final catch roll.
   On success:
     → Row inserted into player_fish_inventory (userId, shopItemId, inAquarium=false).
     → If first time catching this species: row inserted into player_fish_catch_log.
     → If bait was equipped: user_inventory.quantity decremented for bait.

6. PLAYER SELLS OR AQUARIUMS THE FISH
   SELL: DELETE from player_fish_inventory, add coins to users.coins.
   AQUARIUM: PATCH player_fish_inventory.in_aquarium = true.

7. FISH BOOK (COLLECTION)
   GET /api/fishing/catch-log  → returns player_fish_catch_log rows.
   Frontend compares against all available fish (shop_items WHERE fishingType="fish")
   to show which species have been caught and which remain undiscovered.

================================================================================
SECTION 15 — TABLE RELATIONSHIP QUICK REFERENCE
================================================================================

  users
    ├── user_inventory          (owns items & pets)
    ├── player_fish_inventory   (caught fish)
    ├── player_fish_catch_log   (first-catch records)
    ├── player_fishing_equipment (active pole & bait)
    ├── player_market_listings  (as seller or buyer)
    ├── pvp_battles             (battle history)
    ├── pvp_battle_groups       (PvP team selection)
    ├── pet_house_positions     (pet placement in house)
    ├── world_pet_positions     (pet on world map)
    ├── friendships             (as requester or receiver)
    ├── notifications
    ├── user_rewards
    ├── user_badges
    ├── badge_reward_claims
    └── coin_purchases

  shop_items
    ├── user_inventory          (owned instances)
    ├── pond_fish               (fish in ponds)
    ├── player_fish_inventory   (caught fish species)
    ├── player_fish_catch_log
    ├── enemy_drops             (loot table)
    ├── reward_bundle_items     (admin reward contents)
    ├── player_market_listings  (market listings)
    └── fish_template_parts     (fish visual parts)

  shop_items (pet type)
    └── pet_templates           (via shop_items.pet_template_id)
          └── pet_template_parts

  user_inventory (pet row)
    └── pet_equipped_accessories (up to 3 accessory rows)

  world_locations
    ├── shop_items              (items sold here)
    ├── pond_fish               (fish catchable here)
    └── location_enemies        (enemies encountered here)
          └── enemy_drops

  worlds
    ├── world_locations
    ├── world_buildings
    ├── world_decor_items → world_decor_placements
    ├── world_pet_positions
    └── fish_barrels

================================================================================
END OF MASTER GAME DOCUMENT
================================================================================
