import type { ClearingGroundDrop as Drop } from "@shared/clearingEquipment";
import { CLEARING_DROP_RARITY_STYLES } from "@/lib/clearingRarityStyles";

export default function ClearingGroundDrop({drop,state="available"}:{drop:Drop;state?:string}){const style=CLEARING_DROP_RARITY_STYLES[drop.stars];return <div data-testid={`clearing-ground-drop-${drop.stars}`} aria-label={`${drop.stars} star Clearing Ground Drop`} className="absolute pointer-events-none animate-clearing-drop" style={{left:`${drop.worldX*100}%`,top:`${drop.worldY*100}%`,width:54,height:54,transform:"translate(-50%,-70%)",zIndex:7,opacity:state==="collecting"?.65:1}}><div className="absolute inset-2 rounded-full" style={{background:style.color,opacity:.28,boxShadow:style.shadow}}/>{drop.stars===5&&[0,1,2,3].map(i=><i key={i} data-testid="clearing-gold-mote" className="absolute h-1.5 w-1.5 rounded-full bg-yellow-200 animate-pulse" style={{left:`${12+i*10}px`,top:`${4+(i%2)*35}px`}}/>)}{drop.imageUrl?<img src={drop.imageUrl} alt="" className="relative h-11 w-11 mx-auto object-contain" draggable={false}/>:<div className="relative mx-auto mt-2 h-8 w-8 rounded-full border border-white/70"/>}<div className="relative -mt-1 text-center text-[9px] tracking-[-2px]" style={{color:style.color,textShadow:"0 1px 2px #000"}}>{"★".repeat(drop.stars)}</div></div>}
